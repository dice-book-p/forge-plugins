---
name: test
description: "실행 테스트 — verify 통과 후 에이전트팀으로 브라우저 자동화(UI/UX) + API 테스트를 실제 실행하여 동작을 검증합니다. verify PASS 후 자동 트리거."
---

verify(코드 검증) 통과 후, 실제 런타임에서 동작을 검증합니다.
브라우저 자동화로 UI/UX를 테스트하고, API 엔드포인트를 호출하여 결과를 확인합니다.

## 선행 조건 검사

실행 전 반드시 확인:
1. 현재 세션에 바인딩된 상태 파일 탐색 → `.forge-flow/state/`에서 `session_id`가 현재 세션(`${CLAUDE_SESSION_ID}`)과 일치하는 `{task_id}.json` 파일 탐색 → 없으면: "워크플로가 시작되지 않았습니다. `/forge-flow:clarify`로 시작하세요."
2. phase가 `"verified"`, `"testing"`, 또는 `"awaiting_manual_result"`인지 확인 → 아니면: "현재 `{phase}` 단계입니다. verify를 먼저 통과하세요."
3. `design_file`이 존재하는 파일 경로인지 확인 → 없으면: "설계 문서를 찾을 수 없습니다."

## 상태 파일 갱신

실행 시작 시:
```json
{ "phase": "testing" }
```

> **rework_counts 독립 추적**: 상태 파일의 `rework_counts.test`를 사용합니다 (다른 스킬의 카운터와 독립).
> 최초 진입 판단: 이전 phase가 `"verified"`이고 **`rework_counts.test`가 0 (또는 미존재)**이면 최초 진입 → `rework_counts.test: 0`. 그 외(`rework_counts.test > 0`)이면 재진입 → 유지.
> **verify REWORK와의 독립성**: verify PASS 시 `rework_counts.verify`만 리셋되고 `rework_counts.test`는 영향 없습니다. test REWORK 이력이 verify를 거쳐 돌아와도 보존됩니다.

## 검증 설정 확인

design 문서의 `## 검증 설정` 섹션을 확인합니다. 검증 강도에 따라 테스트 수준이 결정됩니다:

- **표준 (1)**: 단일 테스터 (기본).
- **강화 (2+)**: 테스트 팀 구성 (테스트 도메인 분리).

> 검증 강도가 미설정이면 규모 기반 기본값을 적용합니다 (S=1, M=1, L=2).

## 스킵 조건

design 문서의 `## 검증 설정`에��� `테스트 방식`을 확인합니다.

1. `테스트 방식: 스킵` → test를 스킵합니다. `test_skip_reason`이 있으면 함께 기록.
2. 미설정이면 아래 **모두** 해당 시 자동 스킵:
   - 규모가 S
   - design 문서에 UI/UX 관련 AC가 없음
   - CLAUDE.md에 `## 테스트 환경` 섹션이 없음

스킵 시:
```
[test 스킵] {사유} → 실행 테스트 불필요
```

## 테스트 방식 분기

design 문서의 `## 검증 설정`에서 `테스트 방식`을 읽고 분기합니다:

| 테스트 방식 | 실행 경로 |
|-----------|----------|
| **자동** | 현행 에이전트팀 로직 (아래 "테스트 팀" 섹션) |
| **수동** | 사용자 직접 테스트 (아래 "수동 테스트" 섹션) |
| **혼합** | 자동화 가능 AC → 에이전트팀, 나머지 → 사용자 (아래 "혼합 테스트" 섹션) |
| **스킵** | 위 스킵 조건에서 처리됨 |

**설정 확인** (최초 진입 1회만):
test 최초 진입 시 현재 테스트 방식�� 표시합니다:
```
[테스트 설정 확인]
  테스트 방식: 자동
  변���하시겠습니까? [Y/n]
```
변경 요청 시 design 문서도 갱신합니다. **REWORK 후 재진입 시에는 묻지 않습니다.**

## 테스트 범위 분석

design 문서의 AC 항목을 분석하여 테스트 유형을 결정합니다:

| AC 유형 | 테스트 방식 | 예시 |
|---------|-----------|------|
| UI 동작 | 브라우저 자동화 | "버튼 클릭 시 모달이 열린다" |
| 페이지 표시 | 브라우저 자동화 | "목록에 10개 항목이 표시된다" |
| API 응답 | API 직접 호출 | "POST /api/users → 201 반환" |
| 데이터 처리 | API + DB 확인 | "주문 생성 시 재고가 감소한다" |
| 폼 유효성 | 브라우저 자동화 | "이메일 형식 오류 시 에러 표시" |

## 테스트 환경 준비

테스터 프롬프트의 `## 테스트 환경` 섹션을 채울 때:
1. 글로벌/프로젝트 CLAUDE.md의 브라우저 테스트 관련 지침을 확인
2. 테스트 도구 선택 규칙이 있으면 해당 내용을 Read하여 프롬프트에 포함
3. 프로젝트 CLAUDE.md의 `## 테스트 환경` 섹션이 있으면 함께 포함

## 테스트 실행 방식

### 테스트 방식 결정

아래 의사결정 트리를 **순서대로** 확인하여 방식을 결정합니다:

```
1. 스킵 조건 해당 → test 스킵 (/forge-flow:complete 호출)
2. 검증 강도 2+ → 도메인 무관 복수 구성 강제
3. 도메인 2개 이상 → 팀원 복수 구성
4. 도메인 1개 → 팀원 1명 구성
```

### 테스트 팀 (에이전트팀)

#### 테스트 팀 구성 분석

design 문서의 AC를 분석하여 독립 테스트 도메인을 식별합니다.

**검증 관점 결정** (팀 구성 전):
1. **기본 도메인**: AC 분석 기반 도메인/유형/시나리오 분리 (아래 표)
2. **프로젝트 관점**: CLAUDE.md `### 프로젝트 검증 관점`에서 `적용 단계`에 `test`가 포함된 관점 (예: 접근성 → 키보드 네비게이션 테스트). **섹션 없으면 기본 도메인만 사용** (폴백)
3. **작업 관점**: design 문서 `## 검증 관점`에서 활성화된 관점

> 전체 테스터 수 상한: **최대 5명**. 초과 시 통합 우선순위: 작업 특화 관점 → 프로젝트 관점 순으로 가장 관련성 높은 기본 도메인에 통합 배정. 그래도 초과 시 AC 연관도가 낮은 프로젝트 관점부터 제외.
> S규모(스킵 아닌 경우): 프로젝트 관점이 있어도 **AC 연관도가 가장 높은 1개만** 통합 테스터에 포함.

도메인 분리 기준:

| 분리 기준 | 예시 |
|----------|------|
| **도메인 분리** | 사용자단 UI 테스트 / 관리자단 UI 테스트 |
| **유형 분리** | 브라우저 UI 테스트 / API 통합 테스트 |
| **시나리오 분리** | 정상 플로우 / 에러·엣지케이스 플로우 |

#### 테스트 팀 구성 보고

```
[테스트 팀 구성]
  테스터 A: 사용자단 UI 플로우 — AC-1, AC-3 담당
  테스터 B: 관리자단 UI 플로우 — AC-2, AC-4 담당
  테스터 C: API 통합 테스트 — AC-5, AC-6 담당

테스트 팀을 구성합니다.
```

#### 에이전트팀 spawn 절차

1. **`TeamCreate` 호출**: `{ "team_name": "test-{task_id}", "description": "테스트 팀" }`
2. **`TaskCreate`로 테스트 작업 생성**: 각 도메인별 작업
3. **`Agent` 도구 + `team_name` + `name` + `model: "sonnet"` 파라미터로 팀원 spawn**
4. 팀원 메시지 자동 수신 → 숙의 → `SendMessage`로 종료 → `TeamDelete`

테스트 도메인별로 팀원을 동적으로 구성합니다 (`model: "sonnet"`):

**테스터 프롬프트 템플릿**:
```
당신은 {프로젝트명}의 {테스터 전문성}입니다.
담당 영역: {담당 도메인/유형}

> 테스터 전문성 결정: UI 테스트 → "프론트엔드 QA 테스터 — 브라우저 자동화로 사용자 플로우를 검증합니다" / API 테스트 → "백엔드 QA 테스터 — HTTP 엔드포인트 응답을 검증합니다" / 프로젝트 관점(접근성) → "접근성 QA 테스터 — WCAG 기준으로 키보드·스크린리더 접근성을 검증합니다"

## 프로젝트 컨텍스트
{CLAUDE.md에서 기술 스택, 빌드 명령, 프로젝트 구조 요약 — 3줄 이내}

## design 문서 (발췌)
{design 문서에서 ## 요구사항, ## 인수 조건 (AC), ## 검증 방법 섹션만 발췌}

## 담당 테스트 AC
{이 테스터가 담당하는 AC 항목만}

## 테스트 환경
{테스트 환경 준비 단계에서 수집한 도구 선택 규칙 + 환경 정보}

## 테스트 방침
1. 담당 AC 항목별로 실제 동작을 확인합니다
2. 브라우저 자동화: 페이지 이동, 클릭, 입력, 결과 확인
3. API 테스트: 엔드포인트 호출, 응답 코드/바디 검증
4. 정상 경로 + 비정상 경로(에러 케이스) 모두 테스트
5. 스크린샷/응답 로그를 근거로 첨부

## 이 프로젝트에서 반복되는 실수 패턴 (해당 시)
{rework-log에서 이번 작업 영향 범위와 관련된 [코드], [환경] 차원의 ×2+ 항목 발췌}

위 패턴에 특히 주의하여 테스트하세요.

## 응답 원칙
- PASS 항목은 한 줄로 요약 (상세 불필요)
- FAIL 항목만 상세히: 기대 vs 실제 + 스크린샷/로그
- 전체 응답 2,000 토큰 이내 권장

## 응답 형식
각 AC 항목에 대해:
- PASS: <실행 결과 근거>
- FAIL: <기대 동작 vs 실제 동작 + 스크린샷/로그>

## 종합 판정
PASS / CONCERNS / REWORK / FAIL + 근거
```

> **핵심 원칙**: 테스터 간 결과를 공유하지 않음 (독립 테스트 보장).

#### 상태 파일 확장 (팀 활성화 시)

```json
{
  "phase": "testing",
  "agent_team": {
    "enabled": true,
    "members": [
      {"role": "tester", "scope": "사용자단 UI", "ac": ["AC-1", "AC-3"], "status": "active"},
      {"role": "tester", "scope": "관리자단 UI", "ac": ["AC-2", "AC-4"], "status": "active"},
      {"role": "tester", "scope": "API 통합", "ac": ["AC-5", "AC-6"], "status": "active"}
    ]
  }
}
```

## 결과 종합

**테스트 팀 — 숙의(Deliberation)** (리더가 종합):
1. 모든 테스터의 결과 수집
2. AC × 테스터 매트릭스 작성
3. **불일치 식별**: 동일 AC를 복수 테스터가 검증한 경우, 판정이 다른 항목 목록화
4. **갈등 분석** (불일치 항목이 있을 때):
   - 테스트 환경 차이인지, 실제 결함인지 판별
   - 근거: 스크린샷/로그 증거 비교
5. **판정 근거 작성**: 최종 판정에 대해 "왜 이 결정을 내렸는지" 기록
6. 실패한 테스터만 재실행 가능 (전체 재실행 불필요)
7. 최종 판정 도출

**숙의 결과 보고 형식**:
```
[숙의 결과]
  일치 항목: AC-1(PASS), AC-5(PASS), ...
  불일치 항목:
    - AC-3: 테스터-UI(PASS) vs 테스터-에러(FAIL)
      → 채택: FAIL — 에러 경로에서 모달 미표시 (스크린샷 참조)
  판정 근거: AC-3 에러 처리 미구현으로 REWORK 판정
```

## 수동 테스트

테스트 방식이 "수동"인 경우:

1. design 문서의 AC별 테스트 시나리오 체크리스트를 생성합니다:
```
[수동 테스트 체크리스트]
  AC-1: {설명}
    ☐ 단계 1: {구체적 조작}
    ☐ 단계 2: {확���할 결과}
    → 기대 결과: {정확한 기대값}

  AC-2: {설명}
    ☐ 단계 1: ...
    → 기대 결과: ...
```

2. **AskUserQuestion 호출**:
```
question: "위 체크리스트에 따라 테스트를 진행해주세요. 완료 후 각 AC의 PASS/FAIL 결과를 알려주세요. 실패한 항목이 있으면 기대 결과 vs 실제 결과를 설명해주세요."
header: "수동 테스트"
```

3. 사용자 미응답 + ��션 종료 → 상태 파일에 `phase: "awaiting_manual_result"` 기록
4. 세션 재개 시 workflow-state.sh hook이 감지 → "수동 테스트 결과를 입력해주세요" ���롬프트

> 수동 테스트의 수렴 라운드는 **1회 고정** (사용자 반복 테스트 부담 방지).

## 혼합 테스트

테스트 방식이 "혼합"인 경우:

1. AC를 자동화 가능/불가로 분류합니다:
   - **자동화 가능**: 명확한 입출력 검증 (API 응답, 요소 존재/텍스트 확인, 폼 제출)
   - **자동화 불가**: 시각적 레이아웃, 애니메이션, 접근성, 주관적 UX

2. **자동화 가능 AC** → 에이전트팀 테스트 (위 "테스트 팀" 섹션과 동일)
3. 자동화 완료 후 **자동화 불가 AC** → 수동 테스트 (위 "수동 테스트" 섹션과 동일)

4. **결과 종합**:
   - 자동 PASS + 수동 PASS → 전체 PASS
   - 자동 FAIL → 전체 REWORK (코드 수정 필요)
   - 자동 PASS + 수동 FAIL → 원인 분류:
     - 에이전트가 1차 분류 제안: "코드 변경 필요" vs "UI/스타일만"
     - AskUserQuestion으로 사용자 확인
     - **코드 변경 필요** → 전체 REWORK (자동 + 수동 재실행)
     - **UI/스타일만** → 수동 항��만 수정 후 수동 재확인

> 혼합 모드 수렴: 자동화 부분만 N회 수렴, 수동 부분 1회 고정.
> `convergence_round`는 자동화 파트만 추적합니다.

## 품질 게이트

| 등급 | 기준 | 조치 |
|------|------|------|
| **통과**(PASS) | 전 테스트 항목 통과 | 수렴 검증 → 확정 |
| **주의**(CONCERNS) | 경미한 UI 이슈 (스타일 미세 차이 등) | `AskUserQuestion`으로 판단 위임 |
| **재작업**(REWORK) | AC 미충족 (기능 미동작, API 에러 등) | 해당 부분 수정 → 재테스트 |
| **실패**(FAIL) | 근본적 문제 (페이지 미로딩, 서버 에러 등) | 이전 단계부터 재검토 |

> **재작업 연속 3회 시 실패로 에스컬레이션.**

### 수렴 검증 (Convergence Verification)

PASS 판정 후, 새로운 테스터를 spawn하여 확인 라운드를 수행합니다. 이전 테스터와 다른 독립 프로세스가 동일 AC를 재테스트합니다.

**수렴 상한 확인**: design 문서의 `## 검증 설정` 섹션에서 `수렴 라운드` 값을 읽습니다.
- 미설정이면 규모 기반 기본값 적용: **S=1, M=1, L=2**
- S 규모는 수렴 1회 고정
- **수동 테스트**: 수렴 1회 고정 (사용자 부담 방지)
- **혼합 테스트**: 자동화 부분만 N회, 수동 부분 1회 고정

**수렴 검증 흐름** (자동 테스트 기준):
1. 테스트 팀 PASS 판정 후, 상태 파일의 `convergence_round` 확인 (없으면 0)
2. `convergence_round` < `convergence_max`인 동안 반복:
   a. 새 테스터 팀 전체 교체 spawn (기존 팀과 별도, 이전 라운드 결과 전달 금지)
   b. 새 팀이 동일 AC 범위를 독립 테스트
   c. **0건** → `convergence_round` += 1 → 수렴 확정 → 최종 PASS (반복 조기 종료)
   d. **이슈 발견** → REWORK 처리 (`convergence_round` **유지**) → 수정 → verify부터 재실행
3. 상한 도달 시 → AskUserQuestion: "계속 수렴" / "현재 상태로 진행"

> 수렴 검증에서 발견된 이슈는 `rework_counts.test` **및** `rework_lifetime.test`에 모두 포함됩니다.
> 수렴 검증자 프롬프트에 이전 라운드 결과를 전달하지 않습니다 (독립 판단 보장).
> 각 수렴 라운드는 테스터 팀 전체를 새로 교체합니다 (동일 팀 재사용 금지).

**주의 판정 시 AskUserQuestion 호출**:
```
question: "경미한 UI 이슈가 발견되었습니다. 어떻게 진행할까요?"
header: "주의"
options:
  - label: "수용 — 진행 (Recommended)"
    description: "경미한 이슈를 인지하고 다음 단계로 진행합니다"
  - label: "수정 후 재테스트"
    description: "이슈를 수정한 뒤 test를 다시 실행합니다"
multiSelect: false
```

**재작업 처리 흐름 (debug-gate 포함)**:

1. 실패한 테스트 항목과 근거(스크린샷/로그)를 사용자에게 보고
2. **debug-gate — 루트코즈 조사** (증상 패치 방지):
   - **Phase 1 체크리스트** (모두 완료해야 수정 착수):
     - [ ] 실패 테스트의 기대값 vs 실제값을 정확히 읽었는가?
     - [ ] 실패를 로컬에서 재현할 수 있는가?
     - [ ] 관련 변경사항을 확인했는가? (git diff)
     - [ ] 데이터/제어 흐름을 역추적했는가?
   - **루트코즈 가설 기록**: 추정 원인을 1문장으로 기록
   - **최소 변경 원칙**: 가설에 대한 단일 최소 수정만 적용
3. **rework-log 기록** (verify SKILL.md의 "Rework Log 관리" 규칙에 따름, 차원 태그 `[코드]` 기본 부여) — 루트코즈 가설 포함
4. 상태 파일 카운터 갱신:
   - `rework_counts.test` +1 (PASS 시 리셋되는 라운드 내 카운터)
   - `rework_lifetime.test` +1 (리셋 없는 전체 누적, `rework_lifetime.test` ≥ 3이면 에스컬레이션 — verify와 동일 프로세스)
5. phase를 `"implementing"`으로 되돌림
6. 코드 수정 (루트코즈 가설 기반 단일 수정)
7. 수정 완료 후 `/forge-flow:verify` → `/forge-flow:test` 재실행

## 테스트 결과 기록

design 문서의 `## 검수 결과` 섹션에 요약 기록, 상세 이력은 `.forge-flow/design/{task_id}.review.md`에 기록:

**design 문서** (`## 검수 결과`):
```markdown
## 검수 결과
- verify: PASS (2026-03-17)
- test: PASS (2026-03-17)
```

**{task_id}.review.md** (상세 이력 — 누적):
```markdown
### test
- #1 REWORK: 사용자 목록 페이지에서 페이지네이션 미동작
  → 수용: PageableResolver 파라미터 바인딩 수정
- #2 PASS
```

## 상태 파일 갱신 (완료 시)

PASS 시:
```json
{ "phase": "tested", "stop_count": 0, "rework_counts": { "test": 0 }, "convergence_round": 0 }
```
> `convergence_round` 0 리셋: test는 워크플로 마지막 검증 단계이므로, PASS 후 complete로 직행합니다. 다음 작업 재사용을 위해 리셋.

수동 테스트 대기 시:
```json
{ "phase": "awaiting_manual_result" }
```

REWORK 시:
```json
{ "phase": "implementing", "rework_counts": { "test": +1 }, "rework_lifetime": { "test": +1 } }
```

FAIL 시:
```json
{ "phase": "clarifying" }
```

## 완료 후 다음 단계

test PASS 후 → 사용자의 추가 입력 없이 **즉시 `/forge-flow:complete`를 호출**합니다.

> `/forge-flow:complete`에서 커밋 여부 확인, design 문서 처리, 워크플로 파일 정리를 수행합니다.
